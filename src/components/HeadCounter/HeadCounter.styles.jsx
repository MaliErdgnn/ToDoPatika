import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        marginTop: 10,
        margin: 5,

    },
    text_and_counter: {
        fontWeight: 'bold',
        fontSize: 35,
        color: 'gold',
    }
});